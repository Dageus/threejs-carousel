# Carousel made in ThreeJS

### How to run

Simply install `NodeJS` on your machine (if you haven't already), and ThreeJS and Vite with:
 - `npm install --save three`
 - `npm install --save-dev vite`

And run with `npx vite`
